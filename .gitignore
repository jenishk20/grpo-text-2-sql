# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
myenv/
py310env/

# Training / eval artifacts (anchored to repo root with leading /, so they do
# NOT match source packages like src/data — that mistake hid src/data/ from git)
/results/
/checkpoints/
/data/
/wandb/
*.out
*.err

# Model weights (live on the cluster, not in git)
*.sqlite
*.safetensors
*.bin

# Env / editor
.env
.DS_Store
.idea/
.vscode/
